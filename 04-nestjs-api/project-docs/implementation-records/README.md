# NestJS API Implementation Records

These are implementation plans, phase reports and tracking notes. The executable application is in `../../04-nestjs-api-app/`.

Files in this folder are intentionally kept outside the source tree so the app folder contains only runtime/build inputs and code.
